import MyComponent from '../components/ipfs'; // Adjust the path as needed

// eslint-disable-next-line @typescript-eslint/explicit-module-boundary-types
function IPFSDataPage() {
  return (
    <div>
      <h1>IPFS Data Page</h1>
      <MyComponent />
    </div>
  );
}

export default IPFSDataPage;
